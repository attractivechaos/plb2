JAVAC=javac
EXE=nqueen.class matmul.class sudoku.class

all:$(EXE)

nqueen.class:nqueen.java
	$(JAVAC) $<

matmul.class:matmul.java
	$(JAVAC) $<

sudoku.class:sudoku.java
	$(JAVAC) $<

clean:
	rm -f $(EXE)
